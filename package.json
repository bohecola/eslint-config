{
  "name": "@bohecola/eslint-config-monorepo",
  "version": "0.1.3",
  "author": "bohecola <bohecola@outlook.com> (https://github/bohecola/)",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "release": "bump package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@bohecola/eslint-config": "*",
    "@jsdevtools/version-bump-prompt": "^6.1.0",
    "eslint": "^8.27.0"
  }
}
